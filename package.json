{
  "name": "delegating-to-external-llm",
  "version": "2.3.0",
  "private": true,
  "description": "Claude Code plugin: Claude is the architect, an external model on your own OpenAI-compatible relay writes all the code.",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "test": "node --test tests/hook.test.mjs tests/spec-lint.test.mjs tests/usage.test.mjs tests/lane-run.test.mjs tests/delegation-context.test.mjs",
    "check": "node --check scripts/llm.mjs && node --check scripts/setup.mjs && node --check scripts/usage.mjs && node --check scripts/usage-log.mjs && node --check scripts/spec-lint.mjs && node --check hooks/block-direct-edits.mjs && node --check hooks/delegation-context.mjs && bash -n scripts/codex-lane.sh && bash -n scripts/lane-worktree.sh"
  },
  "license": "MIT"
}
